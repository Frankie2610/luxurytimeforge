# ZIP Manifest — Sprint 30

Bao gồm source React/TypeScript, CSS, public assets, Firebase example rules, scripts kiểm tra, pnpm/npm lockfile và launcher Windows.

Không bao gồm:

- `node_modules`
- `dist`
- `.env`
- credentials hoặc secret thật
