# Sprint 49.15

- Thêm favicon SVG cho tab trình duyệt.
- Tách CSS Product Editor sang `v4915-product-editor.css`.
- Khôi phục scroll dọc cho trang chỉnh sửa/tạo sản phẩm.
- Tối ưu bố cục editor trên desktop, tablet và mobile.
- Tách Journal hero sang class `tf-journal-hero-v4915`.
- Sửa tương phản nút Áp dụng mã ưu đãi và badge giỏ hàng ở checkout.

Lưu ý: môi trường đóng gói không truy cập được npm registry, vì vậy cần chạy `pnpm install`, `typecheck` và `build` trên máy local trước khi deploy.
