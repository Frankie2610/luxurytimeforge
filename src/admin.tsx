import{useEffect,useMemo,useState,type ButtonHTMLAttributes,type InputHTMLAttributes,type ReactNode,type TextareaHTMLAttributes}from'react';
import{toast as sonnerToast}from'sonner';
import{Link,NavLink,Outlet,useLocation,useNavigate,useParams}from'react-router-dom';
import{DndContext,PointerSensor,useSensor,useSensors,type DragEndEvent}from'@dnd-kit/core';
import{SortableContext,arrayMove,useSortable,rectSortingStrategy}from'@dnd-kit/sortable';
import{CSS}from'@dnd-kit/utilities';
import{Activity,AlertTriangle,ArrowLeft,ArrowUpRight,BadgePercent,BarChart3,Bell,Boxes,CheckCircle2,ChevronDown,CircleDollarSign,CircleUserRound,Copy,Download,Eye,EyeOff,FileText,FileUp,GripVertical,History,Home,ImagePlus,Laptop,LayoutTemplate,Menu,Monitor,MoreHorizontal,PackageCheck,PackageSearch,Plus,Redo2,RotateCcw,Save,Search,Settings,ShoppingBag,Smartphone,Tags,Trash2,Undo2,UploadCloud,UserRoundSearch,Users,Wrench,X}from'lucide-react';
import{useCommerce}from'./context';import{useAuth}from'./auth';
import type{BlockType,Collection,Customer,ImportResult,Metafield,Product,ProductOption,Section,SectionType,TemplateKey,Theme,ThemeBlock,Variant}from'./types';
import{download,exportShopifyCsv,parseShopifyCsv}from'./csv';
import{money,slugify,strip,uid}from'./utils';
import{allowedBlocks,allowedSections,blockLabels,createSection,sectionLabels,templateLabels}from'./theme';
import{ThemePreviewCanvas}from'./theme-renderer';
import{AdminCommandPalette}from'./admin-v9';
import{Tabs,TabsContent,TabsList,TabsTrigger}from'./ui';
import'./v4926-admin.css';
const Btn=({variant='primary',className='',...p}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'ghost'|'danger'})=><button {...p} className={`btn ${variant} ${className}`}/>;
const Input=(p:InputHTMLAttributes<HTMLInputElement>)=><input {...p} className={`input ${p.className||''}`}/>;
const Textarea=(p:TextareaHTMLAttributes<HTMLTextAreaElement>)=><textarea {...p} className={`input textarea ${p.className||''}`}/>;
const Select=({children,...p}:React.SelectHTMLAttributes<HTMLSelectElement>)=><select {...p} className={`input select ${p.className||''}`}>{children}</select>;
const Field=({label,hint,children}:{label:string;hint?:string;children:ReactNode})=><label className="field"><span>{label}</span>{children}{hint&&<small>{hint}</small>}</label>;
const Badge=({children,tone='neutral'}:{children:ReactNode;tone?:'success'|'warning'|'neutral'|'danger'})=><span className={`badge ${tone}`}>{children}</span>;
type ToastTone='success'|'danger'|'info';type ToastPayload={message:string;tone?:ToastTone};const emitToast=(message:string,tone:ToastTone='success')=>window.dispatchEvent(new CustomEvent<ToastPayload>('timeforge:toast',{detail:{message,tone}}));function AdminToastHost(){useEffect(()=>{const onToast=(event:Event)=>{const detail=(event as CustomEvent<ToastPayload>).detail;if(detail.tone==='danger')sonnerToast.error(detail.message);else if(detail.tone==='info')sonnerToast.info(detail.message);else sonnerToast.success(detail.message)};window.addEventListener('timeforge:toast',onToast);return()=>window.removeEventListener('timeforge:toast',onToast)},[]);return null}
function CdnImageField({label,value,onChange}:{label:string;value:string;onChange:(url:string)=>void;folder?:string}){return <Field label={label} hint="Dán URL ảnh HTTPS từ CDN của cửa hàng."><div className="cdn-image-field">{value&&<img src={value} alt="Xem trước ảnh CDN" loading="lazy" decoding="async"/>}<Input type="url" inputMode="url" value={value} onChange={e=>onChange(e.target.value.trimStart())} placeholder="https://cdn.example.com/image.webp"/></div></Field>}

const nav=[['/admin','Trang chủ',Home],['/admin/orders','Đơn hàng',ShoppingBag],['/admin/returns','Hoàn trả',RotateCcw],['/admin/draft-orders','Đơn hàng nháp',FileText],['/admin/products','Sản phẩm',Boxes],['/admin/collections','Bộ sưu tập',Tags],['/admin/inventory','Hàng tồn kho',PackageSearch],['/admin/customers','Khách hàng',Users],['/admin/customer-segments','Phân khúc khách hàng',UserRoundSearch],['/admin/analytics','Phân tích',BarChart3],['/admin/discounts','Mã giảm giá',BadgePercent],['/admin/activity','Hoạt động',Activity],['/admin/import-export','Nhập / xuất',FileUp]] as const;
export function AdminLayout(){const[open,setOpen]=useState(false),loc=useLocation();const{dataSource}=useCommerce();const{user,logout}=useAuth();const liveData=dataSource==='firebase',dataLabel=dataSource==='loading'?'Đang tải Firebase':liveData?'Firebase live':dataSource.includes('fallback')?'Dữ liệu dự phòng':dataSource==='seed'?'Dữ liệu mẫu':'Dữ liệu local';const title=loc.pathname.startsWith('/admin/products/')?'Chỉnh sửa sản phẩm':loc.pathname.startsWith('/admin/draft-orders/')?'Chỉnh sửa đơn nháp':loc.pathname.startsWith('/admin/orders/')?'Chi tiết đơn hàng':({'/admin':'Trang chủ','/admin/draft-orders':'Đơn hàng nháp','/admin/returns':'Hoàn trả','/admin/products':'Sản phẩm','/admin/collections':'Bộ sưu tập','/admin/customers':'Khách hàng','/admin/customer-segments':'Phân khúc khách hàng','/admin/import-export':'Nhập / xuất dữ liệu','/admin/online-store':'Cửa hàng online','/admin/settings':'Cài đặt','/admin/settings/integrations':'Thanh toán & giao hàng','/admin/inventory':'Hàng tồn kho','/admin/orders':'Đơn hàng','/admin/analytics':'Phân tích','/admin/discounts':'Mã giảm giá','/admin/activity':'Nhật ký hoạt động'} as Record<string,string>)[loc.pathname]||'TimeForge Admin';return <div className="admin-app">{open&&<button className="side-backdrop" onClick={()=>setOpen(false)}/>}<aside className={`sidebar ${open?'open':''}`}><div className="side-logo"><Link to="/admin"><span className="shop-mark">TF</span><span className="shop-name">TimeForge<small>Commerce</small></span></Link><button onClick={()=>setOpen(false)} aria-label="Đóng menu"><X/></button></div><div className="store-switcher"><span>TF</span><div><b>TimeForge</b><small>Cửa hàng chính</small></div><ChevronDown/></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/admin'} onClick={()=>setOpen(false)}><Icon/><span>{label}</span></NavLink>)}<div className="side-group"><p>Kênh bán hàng</p><NavLink to="/admin/online-store" onClick={()=>setOpen(false)}><LayoutTemplate/><span>Cửa hàng online</span><ChevronDown/></NavLink></div></nav><div className="side-settings-stack"><NavLink className="side-settings" to="/admin/settings/integrations"><Wrench/>Tích hợp</NavLink><NavLink className="side-settings" to="/admin/settings"><Settings/>Cài đặt</NavLink></div></aside><div className="admin-main"><header className="topbar"><button className="top-menu" onClick={()=>setOpen(true)} aria-label="Mở menu"><Menu/></button><button className="admin-search admin-search-button" onClick={()=>window.dispatchEvent(new Event('timeforge:open-command'))}><Search/><span>Tìm kiếm</span><kbd>Ctrl K</kbd></button><div className="top-right"><span className={`firebase-state ${liveData?'live':''}`}>{dataLabel}</span><Link className="view-store" to="/" target="_blank">Xem cửa hàng</Link><button className="top-icon" aria-label="Thông báo"><Bell/></button><span className="admin-user"><span className="avatar">{(user?.name||'A').slice(0,1).toUpperCase()}</span><span><b>{user?.name}</b><small>{user?.role}</small></span></span><button className="admin-logout" onClick={()=>void logout()}>Đăng xuất</button></div></header><div className="admin-head"><div><small>TimeForge</small><h1>{title}</h1></div></div><div className="admin-content"><Outlet/></div></div><AdminToastHost/><AdminCommandPalette/></div>}
export function Dashboard(){const{products,customers,collections,themeState}=useCommerce(),inventory=products.reduce((s,p)=>s+p.inventory,0),value=products.reduce((s,p)=>s+p.price*p.inventory,0);return <div><section className="welcome"><div><small>Theme đã xuất bản {new Date(themeState.publishedAt).toLocaleDateString('vi-VN')}</small><h2>Quản lý TimeForge trong một nơi.</h2><p>Sprint 2 bổ sung theme đa template, draft/publish và editor sản phẩm nâng cao.</p></div><Link to="/admin/products/new">Thêm sản phẩm<ArrowUpRight/></Link></section><section className="metrics"><Metric icon={<Boxes/>} label="Sản phẩm" value={String(products.length)} note={`${products.filter(p=>p.status==='active').length} đang hoạt động`}/><Metric icon={<PackageCheck/>} label="Tồn kho" value={String(inventory)} note={`${products.filter(p=>p.inventory<=3).length} sắp hết`}/><Metric icon={<Users/>} label="Khách hàng" value={String(customers.length)} note="Hồ sơ đã lưu"/><Metric icon={<CircleDollarSign/>} label="Giá trị hàng" value={money(value)} note="Theo giá bán"/></section><section className="admin-two"><article className="card"><CardHead small="Hoạt động" title="Sản phẩm mới cập nhật" link="/admin/products"/><div className="recent-products">{products.slice(0,5).map(p=><Link to={`/admin/products/${p.id}`} key={p.id}><img src={p.images[0]} alt=""/><div><b>{p.title}</b><span>{p.vendor} · {p.sku||'Chưa SKU'}</span></div><Badge tone={p.status==='active'?'success':'warning'}>{p.status}</Badge></Link>)}</div></article><article className="card"><CardHead small="Online Store" title="Theme đang xuất bản"/><div className="summary-list"><div><span>Tên theme</span><b>{themeState.published.name}</b></div><div><span>Templates</span><b>{Object.keys(themeState.published.templates).length}</b></div><div><span>Phiên bản lưu</span><b>{themeState.versions.length}</b></div><div><span>Bộ sưu tập</span><b>{collections.length}</b></div></div><Link className="card-link" to="/admin/online-store">Tùy chỉnh theme<ArrowUpRight/></Link></article></section></div>}
function Metric({icon,label,value,note}:{icon:ReactNode;label:string;value:string;note:string}){return <article><span>{icon}</span><div><small>{label}</small><b>{value}</b><p>{note}</p></div></article>}
function CardHead({small,title,link}:{small:string;title:string;link?:string}){return <header className="card-head"><div><small>{small}</small><h3>{title}</h3></div>{link&&<Link to={link}>Xem tất cả</Link>}</header>}
export function Products(){const{products,deleteProducts}=useCommerce();const[q,setQ]=useState(''),[status,setStatus]=useState('all'),[selected,setSelected]=useState<string[]>([]);const shown=products.filter(p=>(status==='all'||p.status===status)&&`${p.title} ${p.vendor} ${p.sku}`.toLowerCase().includes(q.toLowerCase())),all=shown.length>0&&shown.every(p=>selected.includes(p.id));return <div className="stack"><div className="admin-actions"><div><Link className="btn secondary" to="/admin/import-export"><FileUp/>Nhập</Link><Link className="btn secondary" to="/admin/import-export"><Download/>Xuất</Link></div><Link className="btn primary" to="/admin/products/new"><Plus/>Thêm sản phẩm</Link></div><section className="card product-card-admin"><div className="tabs">{[['all','Tất cả'],['active','Đang hoạt động'],['draft','Bản nháp'],['archived','Lưu trữ']].map(([v,l])=><button className={status===v?'active':''} key={v} onClick={()=>setStatus(v)}>{l}</button>)}</div><div className="table-tools"><div><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm tên, SKU hoặc thương hiệu"/></div>{selected.length>0&&<Btn variant="danger" onClick={()=>{deleteProducts(selected);setSelected([])}}><Trash2/>Xóa {selected.length}</Btn>}</div><div className="table-wrap"><table><thead><tr><th><input type="checkbox" checked={all} onChange={e=>setSelected(e.target.checked?shown.map(p=>p.id):[])}/></th><th>Sản phẩm</th><th>Trạng thái</th><th>Tồn kho</th><th>Loại</th><th>Thương hiệu</th><th>Giá</th><th/></tr></thead><tbody>{shown.map(p=><tr key={p.id}><td><input type="checkbox" checked={selected.includes(p.id)} onChange={e=>setSelected(e.target.checked?[...selected,p.id]:selected.filter(id=>id!==p.id))}/></td><td><Link className="product-cell" to={`/admin/products/${p.id}`}><img src={p.images[0]} alt=""/><div><b>{p.title}</b><span>{p.sku||'Chưa SKU'}</span></div></Link></td><td><Badge tone={p.status==='active'?'success':p.status==='draft'?'warning':'neutral'}>{p.status}</Badge></td><td>{p.inventory} trong kho</td><td>{p.productType||'—'}</td><td>{p.vendor||'—'}</td><td>{money(p.price)}</td><td><button className="icon-btn"><MoreHorizontal/></button></td></tr>)}</tbody></table></div></section></div>}
export function Collections(){
  const{collections,products,saveCollection,deleteCollection}=useCommerce();
  const[editing,setEditing]=useState<Collection|null>(null),[productQuery,setProductQuery]=useState('');
  const blank=():Collection=>({id:uid('c'),handle:'',title:'',description:'',type:'manual',status:'draft',image:'',productIds:[],conditions:[]});
  const picked=editing?.productIds.length||0;
  const shownProducts=products.filter(p=>`${p.title} ${p.vendor} ${p.sku}`.toLowerCase().includes(productQuery.toLowerCase()));
  return <div className="tf4917-catalog-page tf4917-collections-page">
    <section className="tf4917-catalog-toolbar tf4917-collection-toolbar"><div className="tf4917-catalog-toolbar-copy"><span>BỘ SƯU TẬP</span><b>Tổ chức catalog theo chiến dịch</b><small>{collections.length} bộ sưu tập · nhóm sản phẩm và dùng làm nguồn hiển thị trên cửa hàng.</small></div><div className="tf4917-catalog-actions"><button className="tf4917-action primary" onClick={()=>{setProductQuery('');setEditing(blank())}}><Plus/>Tạo bộ sưu tập</button></div></section>
    {collections.length?<div className="tf4917-collection-grid">{collections.map(c=><article className="tf4917-collection-card" key={c.id}><div className="collection-card-media">{c.image?<img src={c.image} alt={c.title}/>:<div className="collection-placeholder"><ImagePlus/><span>Chưa có ảnh</span></div>}<Badge tone={c.status==='active'?'success':'warning'}>{c.status==='active'?'Hoạt động':'Bản nháp'}</Badge></div><div className="collection-card-body"><div><h3>{c.title}</h3><p>{c.description||'Chưa có mô tả cho bộ sưu tập này.'}</p></div><div className="collection-card-meta"><span>{c.type==='manual'?'Thủ công':'Tự động'}</span><b>{c.type==='manual'?`${c.productIds.length} sản phẩm`:`${c.conditions.length} điều kiện`}</b></div><footer><Btn variant="secondary" onClick={()=>{setProductQuery('');setEditing(structuredClone(c))}}>Chỉnh sửa</Btn><button className="danger-icon" aria-label="Xóa bộ sưu tập" onClick={()=>{deleteCollection(c.id);emitToast('Đã xóa bộ sưu tập.','info')}}><Trash2/></button></footer></div></article>)}</div>:<section className="tf4917-collection-empty"><Tags/><h2>Chưa có bộ sưu tập</h2><p>Tạo bộ sưu tập đầu tiên để nhóm sản phẩm và hiển thị trên storefront.</p><Btn onClick={()=>setEditing(blank())}><Plus/>Tạo bộ sưu tập</Btn></section>}
    {editing&&<div className="collection-modal-shell"><button className="collection-modal-backdrop" aria-label="Đóng" onClick={()=>setEditing(null)}/><section className="collection-modal tf4917-collection-modal"><header><div><small>BỘ SƯU TẬP</small><h2>{collections.some(c=>c.id===editing.id)?'Chỉnh sửa bộ sưu tập':'Tạo bộ sưu tập'}</h2></div><button className="icon-btn" onClick={()=>setEditing(null)}><X/></button></header><div className="collection-modal-body"><div className="collection-form-main"><section className="collection-form-card"><Field label="Tên bộ sưu tập"><Input autoFocus value={editing.title} onChange={e=>setEditing({...editing,title:e.target.value,handle:editing.handle||slugify(e.target.value)})} placeholder="Ví dụ: Đồng hồ nam"/></Field><Field label="Mô tả"><Textarea rows={5} value={editing.description} onChange={e=>setEditing({...editing,description:e.target.value})} placeholder="Giới thiệu ngắn gọn về nhóm sản phẩm"/></Field><Field label="URL handle"><div className="handle"><span>collections/</span><Input value={editing.handle} onChange={e=>setEditing({...editing,handle:slugify(e.target.value)})}/></div></Field></section><section className="collection-form-card"><div className="collection-section-head"><div><h3>Sản phẩm</h3><p>{editing.type==='manual'?`${picked} sản phẩm đã chọn`:'Sản phẩm được thêm theo điều kiện tự động.'}</p></div></div>{editing.type==='manual'?<><div className="product-picker-search"><Search/><Input value={productQuery} onChange={e=>setProductQuery(e.target.value)} placeholder="Tìm sản phẩm, SKU hoặc thương hiệu"/></div><div className="product-picker">{shownProducts.map(p=><label key={p.id}><input type="checkbox" checked={editing.productIds.includes(p.id)} onChange={e=>setEditing({...editing,productIds:e.target.checked?[...editing.productIds,p.id]:editing.productIds.filter(x=>x!==p.id)})}/><img src={p.images[0]} alt=""/><span><b>{p.title}</b><small>{p.vendor||'Không có thương hiệu'} · {p.sku||'Chưa SKU'}</small></span><strong>{money(p.price)}</strong></label>)}{!shownProducts.length&&<p className="picker-empty">Không tìm thấy sản phẩm phù hợp.</p>}</div></>:<div className="automatic-note"><Wrench/><div><b>Điều kiện tự động</b><p>Trình tạo điều kiện nâng cao sẽ được bổ sung ở sprint tương tác tiếp theo.</p></div></div>}</section></div><aside className="collection-form-side"><section className="collection-form-card"><h3>Trạng thái</h3><Select value={editing.status} onChange={e=>setEditing({...editing,status:e.target.value as Collection['status']})}><option value="active">Hoạt động</option><option value="draft">Bản nháp</option></Select></section><section className="collection-form-card"><h3>Loại bộ sưu tập</h3><Select value={editing.type} onChange={e=>setEditing({...editing,type:e.target.value as Collection['type']})}><option value="manual">Thủ công</option><option value="automatic">Tự động</option></Select></section><section className="collection-form-card"><CdnImageField label="Ảnh đại diện" value={editing.image} folder={`timeforge/collections/${editing.handle||editing.id}`} onChange={image=>setEditing({...editing,image})}/></section></aside></div><footer><Btn variant="secondary" onClick={()=>setEditing(null)}>Hủy</Btn><Btn disabled={!editing.title.trim()} onClick={()=>{saveCollection({...editing,handle:editing.handle||slugify(editing.title)});emitToast('Đã lưu bộ sưu tập.');setEditing(null)}}><Save/>Lưu bộ sưu tập</Btn></footer></section></div>}
  </div>
}
export function Customers(){const{customers,saveCustomer}=useCommerce();const[editing,setEditing]=useState<Customer|null>(null),blank=():Customer=>({id:uid('u'),name:'',email:'',phone:'',ordersCount:0,totalSpent:0,tags:[],createdAt:new Date().toISOString()});return <div className="stack"><div className="admin-actions"><p>Hồ sơ khách hàng được chuẩn bị để đồng bộ Firebase.</p><Btn onClick={()=>setEditing(blank())}><Plus/>Thêm khách hàng</Btn></div><section className="card table-wrap"><table><thead><tr><th>Khách hàng</th><th>Email</th><th>Đơn hàng</th><th>Đã chi</th><th/></tr></thead><tbody>{customers.map(c=><tr key={c.id}><td><b>{c.name}</b><br/><small>{c.phone}</small></td><td>{c.email}</td><td>{c.ordersCount}</td><td>{money(c.totalSpent)}</td><td><Btn variant="ghost" onClick={()=>setEditing(structuredClone(c))}>Sửa</Btn></td></tr>)}</tbody></table></section>{editing&&<div className="modal-shell"><button className="modal-backdrop" onClick={()=>setEditing(null)}/><section className="modal card"><header><h2>Khách hàng</h2><button onClick={()=>setEditing(null)}><X/></button></header><Field label="Họ tên"><Input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/></Field><Field label="Email"><Input value={editing.email} onChange={e=>setEditing({...editing,email:e.target.value})}/></Field><Field label="Số điện thoại"><Input value={editing.phone} onChange={e=>setEditing({...editing,phone:e.target.value})}/></Field><footer><Btn variant="secondary" onClick={()=>setEditing(null)}>Hủy</Btn><Btn onClick={()=>{saveCustomer(editing);setEditing(null)}}>Lưu</Btn></footer></section></div>}</div>}
export function ImportExport(){
  const{products,headers,setHeaders,mergeProducts,replaceProducts,firebaseEnabled}=useCommerce();
  const[file,setFile]=useState<File|null>(null),[preview,setPreview]=useState<ImportResult|null>(null),[publish,setPublish]=useState(true),[mode,setMode]=useState<'merge'|'replace'>('merge'),[busy,setBusy]=useState(false),[error,setError]=useState('');
  const inspect=async(f:File|null)=>{setFile(f);setPreview(null);setError('');if(!f)return;setBusy(true);try{setPreview(await parseShopifyCsv(f,publish))}catch(e){setError(e instanceof Error?e.message:'Không đọc được CSV')}finally{setBusy(false)}};
  const apply=async()=>{if(!file)return;setBusy(true);setError('');try{const result=await parseShopifyCsv(file,publish);if(mode==='replace')await replaceProducts(result.products);else await mergeProducts(result.products);setHeaders(result.headers);setPreview(result);emitToast(firebaseEnabled?`Đã lưu ${result.products.length} sản phẩm lên Firebase.`:`Đã nhập ${result.products.length} sản phẩm vào dữ liệu local.`)}catch(reason){const message=reason instanceof Error?reason.message:'Không thể nhập CSV.';setError(message);emitToast(message,'danger')}finally{setBusy(false)}};
  const updating=preview?.products.filter(p=>products.some(x=>x.handle===p.handle||(p.sku&&x.sku===p.sku))).length||0,newCount=(preview?.products.length||0)-updating;
  return <div className="import-export-page"><section className="card import-card import-card-primary"><header className="import-card-head"><div className="import-icon"><FileUp/></div><div><h2>Nhập CSV vào Firebase theo SKU</h2><p>Tự nhận diện file Shopify hoặc CSV thường, lấy URL hình ảnh từ các cột ảnh và dùng SKU làm ID sản phẩm.</p></div></header><label className={`upload-file ${file?'has-file':''}`}><input type="file" accept=".csv,text/csv" onChange={e=>void inspect(e.target.files?.[0]||null)}/><UploadCloud/><div><b>{file?.name||'Thả file CSV vào đây'}</b><span>{busy?'Đang phân tích dữ liệu...':file?'Bấm để chọn file khác':'Hoặc bấm để chọn từ máy'}</span></div></label>{error&&<div className="alert error"><AlertTriangle/>{error}</div>}{preview&&<><div className="import-preview-stats"><article><span>Headers</span><b>{preview.headers.length}</b></article><article><span>Dòng dữ liệu</span><b>{preview.rowCount}</b></article><article><span>Sản phẩm</span><b>{preview.products.length}</b></article><article><span>Bản nháp</span><b>{preview.draftCount}</b></article></div><div className="conflict-preview"><div className="conflict-head"><div><h3>Xem trước thay đổi</h3><p><b>{newCount}</b> mới · <b>{updating}</b> cập nhật</p></div><Badge tone={updating?'warning':'success'}>{updating?'Có dữ liệu trùng':'Sẵn sàng'}</Badge></div><div className="conflict-list">{preview.products.slice(0,6).map(p=>{const exists=products.some(x=>x.handle===p.handle||(p.sku&&x.sku===p.sku));return <article className="conflict-product-card" key={p.id}><div className="conflict-product-media"><img src={p.images[0]} alt={p.title}/></div><div className="conflict-product-copy"><b>{p.title}</b><small>SKU {p.sku} · /timeforge/products/{p.sku}</small></div><Badge tone={exists?'warning':'success'}>{exists?'Cập nhật':'Mới'}</Badge></article>})}</div>{preview.warnings.length>0&&<div className="alert warning"><AlertTriangle/><div><b>Cần kiểm tra</b>{preview.warnings.slice(0,5).map(item=><small key={item}>{item}</small>)}</div></div>}{preview.products.length>6&&<p className="more-items">Và {preview.products.length-6} sản phẩm khác</p>}</div></>}<div className="import-settings"><label className="check"><input type="checkbox" checked={publish} onChange={e=>{setPublish(e.target.checked);if(file)void inspect(file)}}/><span><b>Đăng sản phẩm lên cửa hàng</b><small>Chuyển sản phẩm draft thành active sau khi nhập.</small></span></label><Field label="Xử lý sản phẩm trùng"><Select value={mode} onChange={e=>setMode(e.target.value as typeof mode)}><option value="merge">Cập nhật và giữ dữ liệu hiện có</option><option value="replace">Thay toàn bộ catalog</option></Select></Field></div><footer className="import-actions"><span>{preview?`${preview.products.length} sản phẩm sẵn sàng · ${firebaseEnabled?'sẽ ghi Firebase':'đang ở chế độ local'}`:'Chọn CSV để bắt đầu'}</span><Btn disabled={!preview||busy} onClick={()=>void apply()}>{busy?'Đang xử lý':`Nhập ${preview?.products.length||0} sản phẩm`}</Btn></footer></section><aside className="import-export-side"><section className="card import-card export-card"><header className="import-card-head"><div className="import-icon"><Download/></div><div><h2>Xuất Shopify CSV</h2><p>Xuất catalog theo header gần nhất đã nhập.</p></div></header><div className="export-summary"><div><span>Sản phẩm</span><b>{products.length}</b></div><div><span>Header</span><b>{headers.length||'Mặc định'}</b></div><div><span>Variants</span><b>{products.reduce((sum,p)=>sum+p.variants.length,0)}</b></div></div><Btn onClick={()=>download(exportShopifyCsv(products,headers),'timeforge-products.csv')}><Download/>Xuất tất cả sản phẩm</Btn><p className="fine">Metafield và cột chưa nhận diện vẫn được giữ từ rawShopify khi có.</p></section><section className="card import-help"><h3>Chuẩn dữ liệu khuyên dùng</h3><ol><li>Mỗi sản phẩm phải có <b>SKU duy nhất</b>.</li><li>Cột ảnh có thể là <b>Image Src, Image URL, Hình ảnh</b> hoặc nhiều cột ảnh.</li><li>SKU không được chứa <b>. # $ [ ] /</b> vì đây là ký tự cấm trong Firebase.</li></ol></section></aside></div>
}
function SortableBlockNode({block,onSelect}:{block:ThemeBlock;onSelect:()=>void}){const{attributes,listeners,setNodeRef,transform,transition}=useSortable({id:block.id});return <button ref={setNodeRef} style={{transform:CSS.Transform.toString(transform),transition}} onClick={onSelect}><span className="block-grip" {...attributes} {...listeners}><GripVertical/></span>{blockLabels[block.type]}{!block.visible&&<EyeOff/>}</button>}
function SortableSection({section,selected,onSelect,onToggle,onDuplicate,onDelete,onBlock,onBlocksReorder}:{section:Section;selected:boolean;onSelect:()=>void;onToggle:()=>void;onDuplicate:()=>void;onDelete:()=>void;onBlock:(id:string)=>void;onBlocksReorder:(active:string,over:string)=>void}){const{attributes,listeners,setNodeRef,transform,transition}=useSortable({id:section.id}),blockSensors=useSensors(useSensor(PointerSensor,{activationConstraint:{distance:4}}));return <article ref={setNodeRef} style={{transform:CSS.Transform.toString(transform),transition}} className={`theme-section-node ${selected?'selected':''}`}><div className="theme-section-row" onClick={onSelect}><button className="drag-handle" {...attributes} {...listeners}><GripVertical/></button><div><b>{sectionLabels[section.type]}</b><span>{section.visible?'Đang hiển thị':'Đang ẩn'} · {section.blocks.length} block</span></div><aside><button onClick={e=>{e.stopPropagation();onToggle()}}>{section.visible?<Eye/>:<EyeOff/>}</button><button onClick={e=>{e.stopPropagation();onDuplicate()}}><Copy/></button><button onClick={e=>{e.stopPropagation();onDelete()}}><Trash2/></button></aside></div>{selected&&section.blocks.length>0&&<DndContext sensors={blockSensors} onDragEnd={e=>{if(e.over&&e.active.id!==e.over.id)onBlocksReorder(String(e.active.id),String(e.over.id))}}><SortableContext items={section.blocks.map(x=>x.id)} strategy={rectSortingStrategy}><div className="block-tree">{section.blocks.map(bl=><SortableBlockNode key={bl.id} block={bl} onSelect={()=>onBlock(bl.id)}/>)}</div></SortableContext></DndContext>}</article>}
const fieldName=(key:string)=>({text:'Nội dung',title:'Tiêu đề',eyebrow:'Nhãn nhỏ',label:'Nhãn nút',link:'Liên kết',image:'Hình ảnh URL',height:'Chiều cao',overlay:'Độ phủ nền',alignment:'Canh nội dung',limit:'Số lượng',columns:'Số cột',gallerySize:'Kích thước gallery',thumbnailPosition:'Vị trí thumbnail',showVendor:'Hiện thương hiệu',showSku:'Hiện SKU',showStock:'Hiện tồn kho',showCompare:'Hiện giá so sánh',showDiscount:'Hiện giảm giá',showBuyNow:'Hiện nút mua ngay',showWishlist:'Hiện yêu thích',source:'Nguồn nội dung',open:'Mở mặc định',background:'Kiểu nền',imagePosition:'Vị trí ảnh',contentWidth:'Chiều rộng website',radius:'Bo góc'} as Record<string,string>)[key]||key;
function SettingInput({k,v,onChange}:{k:string;v:string|number|boolean;onChange:(v:string|number|boolean)=>void}){if(k==='image')return <CdnImageField label={fieldName(k)} value={String(v)} folder="timeforge/theme/sections" onChange={onChange}/>;if(typeof v==='boolean')return <label className="switch-row"><span>{fieldName(k)}</span><input type="checkbox" checked={v} onChange={e=>onChange(e.target.checked)}/></label>;if(['alignment','imagePosition','thumbnailPosition','gallerySize','background','style','source','width','layout'].includes(k))return <Field label={fieldName(k)}><Select value={String(v)} onChange={e=>onChange(e.target.value)}>{k==='alignment'&&<><option value="left">Trái</option><option value="center">Giữa</option><option value="right">Phải</option></>}{k==='imagePosition'&&<><option value="left">Ảnh trái</option><option value="right">Ảnh phải</option></>}{k==='thumbnailPosition'&&<><option value="left">Bên trái</option><option value="bottom">Bên dưới</option></>}{k==='gallerySize'&&<><option value="small">Nhỏ</option><option value="medium">Vừa</option><option value="large">Lớn</option></>}{k==='background'&&<><option value="dark">Tối</option><option value="light">Sáng</option></>}{k==='style'&&<><option value="primary">Nút chính</option><option value="secondary">Nút phụ</option><option value="link">Liên kết</option></>}{k==='source'&&<><option value="description">Mô tả sản phẩm</option><option value="shipping">Giao hàng</option><option value="warranty">Bảo hành</option></>}{k==='width'&&<><option value="narrow">Hẹp</option><option value="wide">Rộng</option></>}{k==='layout'&&<><option value="cards">Thẻ ảnh</option><option value="minimal">Tối giản</option></>}</Select></Field>;if(k==='text')return <Field label={fieldName(k)}><Textarea rows={4} value={String(v)} onChange={e=>onChange(e.target.value)}/></Field>;return <Field label={fieldName(k)}><Input type={typeof v==='number'?'number':'text'} value={String(v)} onChange={e=>onChange(typeof v==='number'?Number(e.target.value):e.target.value)}/></Field>}
export function OnlineStore(){const{draftTheme,themeState,saveThemeDraft,publishTheme,restoreThemeVersion}=useCommerce();const[theme,setTheme]=useState<Theme>(()=>structuredClone(draftTheme)),[template,setTemplate]=useState<TemplateKey>('home'),[selectedSection,setSelectedSection]=useState(theme.templates.home.sections[0]?.id||''),[selectedBlock,setSelectedBlock]=useState(''),[device,setDevice]=useState<'desktop'|'tablet'|'mobile'>('desktop'),[history,setHistory]=useState<Theme[]>([]),[future,setFuture]=useState<Theme[]>([]),[panel,setPanel]=useState<'editor'|'settings'|'history'>('editor');const sensors=useSensors(useSensor(PointerSensor,{activationConstraint:{distance:6}})),current=theme.templates[template],section=current.sections.find(s=>s.id===selectedSection),block=section?.blocks.find(b=>b.id===selectedBlock),dirty=JSON.stringify(theme)!==JSON.stringify(draftTheme);const commit=(next:Theme)=>{setHistory(h=>[...h,structuredClone(theme)].slice(-40));setFuture([]);setTheme(next)};const setSections=(sections:Section[])=>commit({...theme,templates:{...theme.templates,[template]:{...current,sections}}});const patchSection=(next:Partial<Section>)=>section&&setSections(current.sections.map(s=>s.id===section.id?{...s,...next}:s));const patchBlock=(next:Partial<ThemeBlock>)=>section&&block&&patchSection({blocks:section.blocks.map(b=>b.id===block.id?{...b,...next}:b)});const switchTemplate=(key:TemplateKey)=>{setTemplate(key);const first=theme.templates[key].sections[0];setSelectedSection(first?.id||'');setSelectedBlock('')};const drag=(e:DragEndEvent)=>{if(!e.over||e.active.id===e.over.id)return;setSections(arrayMove(current.sections,current.sections.findIndex(s=>s.id===e.active.id),current.sections.findIndex(s=>s.id===e.over?.id)))};const undo=()=>{const prev=history.at(-1);if(!prev)return;setFuture(f=>[structuredClone(theme),...f]);setHistory(h=>h.slice(0,-1));setTheme(prev)};const redo=()=>{const next=future[0];if(!next)return;setHistory(h=>[...h,structuredClone(theme)]);setFuture(f=>f.slice(1));setTheme(next)};const addSection=(type:SectionType)=>{const next=createSection(type);setSections([...current.sections,next]);setSelectedSection(next.id);setSelectedBlock('')};const addBlock=(type:BlockType)=>{if(!section)return;const next:ThemeBlock={id:uid('b'),type,visible:true,settings:type==='heading'?{eyebrow:'',text:'Tiêu đề mới'}:type==='text'?{text:'Nội dung mới'}:type==='button'?{label:'Xem thêm',link:'/collections',style:'primary'}:type==='iconText'?{icon:'shield',title:'Cam kết',text:'Nội dung cam kết'}:type==='accordion'?{title:'Nội dung',source:'description',open:false}:{}};patchSection({blocks:[...section.blocks,next]});setSelectedBlock(next.id)};return <div className="theme-page-v2"><div className="theme-topbar-v2"><div className="theme-template-select"><span>Template</span><Select value={template} onChange={e=>switchTemplate(e.target.value as TemplateKey)}>{(Object.keys(templateLabels) as TemplateKey[]).map(k=><option value={k} key={k}>{templateLabels[k]}</option>)}</Select></div><div className="theme-tabs-v2"><button className={panel==='editor'?'active':''} onClick={()=>setPanel('editor')}>Sections</button><button className={panel==='settings'?'active':''} onClick={()=>setPanel('settings')}>Cài đặt theme</button><button className={panel==='history'?'active':''} onClick={()=>setPanel('history')}>Lịch sử</button></div><div className="devices"><button className={device==='desktop'?'active':''} onClick={()=>setDevice('desktop')}><Monitor/></button><button className={device==='tablet'?'active':''} onClick={()=>setDevice('tablet')}><Laptop/></button><button className={device==='mobile'?'active':''} onClick={()=>setDevice('mobile')}><Smartphone/></button></div><div className="theme-actions-v2"><button disabled={!history.length} onClick={undo}><Undo2/></button><button disabled={!future.length} onClick={redo}><Redo2/></button><span className={dirty?'dirty':'saved'}>{dirty?'Chưa lưu':'Đã lưu draft'}</span><Btn variant="secondary" onClick={()=>saveThemeDraft(theme)}><Save/>Lưu nháp</Btn><Btn onClick={()=>{saveThemeDraft(theme);publishTheme(theme)}}>Xuất bản</Btn></div></div><div className="theme-workspace-v2"><aside className="theme-left-v2">{panel==='editor'&&<><header><small>CẤU TRÚC TEMPLATE</small><h2>{templateLabels[template]}</h2><p>Kéo section để đổi thứ tự. Bấm section hoặc block để chỉnh.</p></header><DndContext sensors={sensors} onDragEnd={drag}><SortableContext items={current.sections.map(s=>s.id)} strategy={rectSortingStrategy}><div className="theme-tree-v2">{current.sections.map(s=><SortableSection key={s.id} section={s} selected={s.id===selectedSection} onSelect={()=>{setSelectedSection(s.id);setSelectedBlock('')}} onBlock={id=>{setSelectedSection(s.id);setSelectedBlock(id)}} onBlocksReorder={(active,over)=>setSections(current.sections.map(x=>x.id===s.id?{...x,blocks:arrayMove(x.blocks,x.blocks.findIndex(b=>b.id===active),x.blocks.findIndex(b=>b.id===over))}:x))} onToggle={()=>setSections(current.sections.map(x=>x.id===s.id?{...x,visible:!x.visible}:x))} onDuplicate={()=>{const copy=structuredClone(s);copy.id=uid('s');copy.blocks=copy.blocks.map(b=>({...b,id:uid('b')}));const a=[...current.sections],i=a.findIndex(x=>x.id===s.id);a.splice(i+1,0,copy);setSections(a)}} onDelete={()=>{if(current.sections.length<=1)return;setSections(current.sections.filter(x=>x.id!==s.id));setSelectedSection(current.sections.find(x=>x.id!==s.id)?.id||'')}}/>)}</div></SortableContext></DndContext><div className="add-section-v2"><b>Thêm section</b>{allowedSections[template].map(type=><button key={type} onClick={()=>addSection(type)}><Plus/>{sectionLabels[type]}</button>)}</div></>}{panel==='settings'&&<div className="global-theme-settings"><header><small>THEME SETTINGS</small><h2>Nhận diện & layout</h2></header><Field label="Tên theme"><Input value={theme.name} onChange={e=>commit({...theme,name:e.target.value})}/></Field><Field label="Tên cửa hàng"><Input value={theme.settings.storeName} onChange={e=>commit({...theme,settings:{...theme.settings,storeName:e.target.value}})}/></Field><Field label="Logo chữ"><Input value={theme.settings.logoText} onChange={e=>commit({...theme,settings:{...theme.settings,logoText:e.target.value}})}/></Field><CdnImageField label="Logo hình" value={theme.settings.logoImage} folder="timeforge/theme/logo" onChange={logoImage=>commit({...theme,settings:{...theme.settings,logoImage}})}/><Field label="Thanh thông báo"><Input value={theme.settings.announcement} onChange={e=>commit({...theme,settings:{...theme.settings,announcement:e.target.value}})}/></Field><label className="switch-row"><span>Hiện thanh thông báo</span><input type="checkbox" checked={theme.settings.showAnnouncement} onChange={e=>commit({...theme,settings:{...theme.settings,showAnnouncement:e.target.checked}})}/></label><label className="switch-row"><span>Header cố định</span><input type="checkbox" checked={theme.settings.stickyHeader} onChange={e=>commit({...theme,settings:{...theme.settings,stickyHeader:e.target.checked}})}/></label><div className="theme-color-grid">{[['accent','Màu nhấn'],['background','Nền trang'],['surface','Nền card'],['text','Màu chữ'],['muted','Chữ phụ']].map(([k,l])=><Field label={l} key={k}><Input type="color" value={String(theme.settings[k as keyof typeof theme.settings])} onChange={e=>commit({...theme,settings:{...theme.settings,[k]:e.target.value}})}/></Field>)}</div><Field label="Bo góc"><Input type="range" min="0" max="32" value={theme.settings.radius} onChange={e=>commit({...theme,settings:{...theme.settings,radius:Number(e.target.value)}})}/></Field><Field label="Chiều rộng nội dung"><Input type="range" min="1100" max="1680" step="20" value={theme.settings.contentWidth} onChange={e=>commit({...theme,settings:{...theme.settings,contentWidth:Number(e.target.value)}})}/></Field></div>}{panel==='history'&&<div className="theme-history"><header><small>VERSION HISTORY</small><h2>Các bản đã xuất bản</h2></header>{!themeState.versions.length?<p>Chưa có phiên bản cũ.</p>:themeState.versions.map(v=><article key={v.id}><History/><div><b>{new Date(v.createdAt).toLocaleString('vi-VN')}</b><span>{v.note}</span></div><Btn variant="secondary" onClick={()=>{restoreThemeVersion(v.id);setTheme(structuredClone(v.theme));setPanel('editor')}}>Khôi phục draft</Btn></article>)}</div>}</aside><main className={`theme-preview-v2 ${device}`}><div><ThemePreviewCanvas theme={theme} template={template}/></div></main><aside className="theme-inspector-v2">{panel==='editor'&&(block?<><header><small>BLOCK</small><h2>{blockLabels[block.type]}</h2><div><button onClick={()=>patchBlock({visible:!block.visible})}>{block.visible?<Eye/>:<EyeOff/>}</button><button onClick={()=>{if(!section)return;const copy={...structuredClone(block),id:uid('b')};const list=[...section.blocks],i=list.findIndex(x=>x.id===block.id);list.splice(i+1,0,copy);patchSection({blocks:list});setSelectedBlock(copy.id)}}><Copy/></button><button onClick={()=>{if(!section)return;patchSection({blocks:section.blocks.filter(x=>x.id!==block.id)});setSelectedBlock('')}}><Trash2/></button></div></header><div className="inspector-fields">{Object.entries(block.settings).map(([k,v])=><SettingInput key={k} k={k} v={v} onChange={value=>patchBlock({settings:{...block.settings,[k]:value}})}/>)}</div></>:section?<><header><small>SECTION</small><h2>{sectionLabels[section.type]}</h2><div><button onClick={()=>patchSection({visible:!section.visible})}>{section.visible?<Eye/>:<EyeOff/>}</button></div></header><div className="inspector-fields">{Object.entries(section.settings).map(([k,v])=><SettingInput key={k} k={k} v={v} onChange={value=>patchSection({settings:{...section.settings,[k]:value}})}/>)}</div>{allowedBlocks[section.type]?.length&&<div className="add-block-v2"><b>Thêm block</b>{allowedBlocks[section.type]?.map(type=><button key={type} onClick={()=>addBlock(type)}><Plus/>{blockLabels[type]}</button>)}</div>}</>:<div className="inspector-empty"><LayoutTemplate/><h3>Chọn section hoặc block</h3><p>Cài đặt phù hợp sẽ xuất hiện tại đây.</p></div>)}</aside></div></div>}
export function Orders(){return <Empty icon={<ShoppingBag/>} title="Chưa kết nối đơn hàng" text="Orders sẽ được kết nối cùng Firebase và checkout ở Sprint tiếp theo."/>}
export function Inventory(){const{products}=useCommerce();return <section className="card table-wrap"><table><thead><tr><th>Sản phẩm</th><th>SKU</th><th>Có sẵn</th><th>Giá trị tồn</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><div className="product-cell"><img src={p.images[0]} alt=""/><b>{p.title}</b></div></td><td>{p.sku||'—'}</td><td>{p.inventory}</td><td>{money(p.inventory*p.price)}</td></tr>)}</tbody></table></section>}
export function Analytics(){const{products,customers}=useCommerce();return <div className="stack"><section className="metrics"><Metric icon={<Boxes/>} label="Đang bán" value={String(products.filter(p=>p.status==='active').length)} note={`Trong ${products.length} sản phẩm`}/><Metric icon={<BarChart3/>} label="Giá trung bình" value={money(products.reduce((s,p)=>s+p.price,0)/Math.max(1,products.length))} note="Catalog hiện tại"/></section><Empty icon={<BarChart3/>} title="Analytics đang ở chế độ nền" text={`Đã có ${customers.length} khách hàng; doanh thu và traffic sẽ nối sau.`}/></div>}
export function SettingsPage(){const{dataSource,dataError,reset,themeState}=useCommerce();const live=dataSource==='firebase',label=dataSource==='loading'?'Đang tải Firebase':live?'Firebase live':dataSource.includes('fallback')?'Đang dùng dữ liệu dự phòng':dataSource==='seed'?'Dữ liệu mẫu':'Local mode';return <div className="settings-grid"><section className="card setting-card"><Wrench/><div><h2>Nguồn dữ liệu</h2><p>{live?'Catalog trên website đang được đọc trực tiếp từ Firebase theo SKU.':dataError||'Catalog hiện chưa được đọc từ Firebase.'}</p><span className={`firebase-state ${live?'live':''}`}>{label}</span><span className="firebase-state live">CDN URL ready</span></div></section><section className="card setting-card"><LayoutTemplate/><div><h2>Theme</h2><p>{themeState.published.name} · {themeState.versions.length} phiên bản lịch sử.</p><Link className="btn secondary" to="/admin/online-store">Mở Theme Editor</Link></div></section><section className="card setting-card"><RotateCcw/><div><h2>Đặt lại dữ liệu demo</h2><p>Khôi phục sản phẩm mẫu và theme mặc định.</p><Btn variant="danger" onClick={()=>{if(confirm('Đặt lại dữ liệu?'))reset()}}>Đặt lại</Btn></div></section></div>}
function Empty({icon,title,text}:{icon:ReactNode;title:string;text:string}){return <section className="card empty"><span>{icon}</span><h2>{title}</h2><p>{text}</p></section>}
